# include "main.h"
/**
 * _isupper - create uppercase
 * @c: function parameter
 * Return: always 0 on failure and 1 on success
 */
int _isupper(int c)

	if (c >= 'A' && c <= 'Z')
	return (1);

	else
	return (0);
